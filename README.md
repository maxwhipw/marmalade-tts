# marmalade-tts Voice Effect Samples

160 voice-effect samples (Kitten TTS, Kiki F + Jasper M).
This branch is a separate orphan branch so the wav files don't bloat the main repo.

See `INDEX.md` for the full catalog with effect chains.

To clone just this branch:
```
git clone -b effect-samples --single-branch http://george:3000/marmalade/marmalade-tts-cli.git samples
```
